#ifndef __MATRIX_SUM__
#define __MATRIX_SUM__

/**
    @brief Prints result of adding two matrices
    @param matrix1 The first matrix
    @param matrix2 The second matrix
    @param sizeY   Vertical size of matrices
    @param sizeY   Horizontal size of matrices
 */
void Matrix_sum (int* matrix1, int* matrix2, const int sizeY, const int sizeX);

#endif
